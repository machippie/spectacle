# Standards: 0.1
---
# @var spectacle_user:description: User to run user-specific commands
# @var spectacle_user:value: spectacle_user | default(homebrew_user) | default(ansible_user_id)
# spectacle_user:

# @var spectacle_started:description: Start in background after install
spectacle_started: True

...
